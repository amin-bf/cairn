[package]
name = "fsrsbench"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
# rlib for the CLI, cdylib for the `.so` the APK loads.
crate-type = ["rlib", "cdylib"]

[dependencies]
fsrs.workspace = true

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.21"
